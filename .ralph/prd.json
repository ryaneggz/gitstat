{
    "project": "GitStat",
    "branchName": "ralph/fix-dev-agent-redirect-port",
    "description": "Fix Dev Agent Login Redirect Port - Validate that dev:agent script correctly sets NEXTAUTH_URL so login redirects to port 3080 instead of 3000",
    "userStories": [
        {
            "id": "US-001",
            "title": "Verify dev:agent script sets NEXTAUTH_URL to port 3080",
            "description": "As a developer, I need to confirm that the dev:agent npm script injects NEXTAUTH_URL=http://localhost:3080 so NextAuth redirects to the correct port.",
            "acceptanceCriteria": [
                "package.json dev:agent script contains 'NEXTAUTH_URL=http://localhost:3080 next dev -p 3080'",
                "Run 'npm run dev:agent' from gitstat-app/ and confirm the server starts on port 3080",
                "Verify in browser using agent-browser skill: navigate to http://localhost:3080 and confirm the page loads",
                "Typecheck passes"
            ],
            "priority": 1,
            "passes": true,
            "notes": "Verified: script already correct, server starts on 3080, browser loads, typecheck passes"
        },
        {
            "id": "US-002",
            "title": "Verify Dev Agent Login redirects to port 3080",
            "description": "As a developer, I need to confirm that clicking 'Dev Agent Login' on the login page redirects to http://localhost:3080/dashboard, not port 3000.",
            "acceptanceCriteria": [
                "Verify in browser using agent-browser skill: navigate to http://localhost:3080/login",
                "Verify in browser using agent-browser skill: the 'Dev Agent Login' button is visible on the login page",
                "Verify in browser using agent-browser skill: click 'Dev Agent Login' button and confirm redirect lands on http://localhost:3080/dashboard (URL bar shows port 3080, not 3000)",
                "No network errors or failed redirects in browser console",
                "Typecheck passes"
            ],
            "priority": 2,
            "passes": true,
            "notes": "Fixed: added NEXTAUTH_SECRET fallback to dev:agent script. Verified browser redirect lands on http://localhost:3080/dashboard"
        },
        {
            "id": "US-003",
            "title": "Verify .example.env has port alignment comment",
            "description": "As a developer, I need documentation in .example.env that explains NEXTAUTH_URL must match the dev server port.",
            "acceptanceCriteria": [
                ".example.env contains a comment mentioning port 3000 for 'npm run dev' and port 3080 for 'npm run dev:agent'",
                "NEXTAUTH_URL=http://localhost:3000 default value is preserved",
                "Typecheck passes"
            ],
            "priority": 3,
            "passes": true,
            "notes": "Verified: .example.env already has port alignment comment on line 10 and NEXTAUTH_URL=http://localhost:3000 default on line 11"
        },
        {
            "id": "US-004",
            "title": "Verify README documents port/NEXTAUTH_URL relationship",
            "description": "As a developer, I need the README to mention port 3080 for dev:agent so new contributors configure OAuth correctly.",
            "acceptanceCriteria": [
                "README.md OAuth callback URL section mentions port 3080 for dev:agent",
                "README.md NEXTAUTH_URL example includes comment about matching dev server port",
                "Typecheck passes"
            ],
            "priority": 4,
            "passes": true,
            "notes": "Verified: README already documents port 3080 for dev:agent in OAuth callback section (line 54) and NEXTAUTH_URL comment (line 74)"
        },
        {
            "id": "US-005",
            "title": "Add startup validation for GITHUB_TOKEN in dev mode",
            "description": "As a developer, I want the app to fail fast at startup with a clear error message when NODE_ENV is not 'production' and GITHUB_TOKEN is not set, so I know exactly what to fix and where.",
            "acceptanceCriteria": [
                "When NODE_ENV !== 'production' and GITHUB_TOKEN is not set (empty or undefined), the app throws an error at startup before serving any requests",
                "The error message includes the text 'GITHUB_TOKEN is required in development mode'",
                "The error message includes the path to the .env file (e.g. 'See gitstat-app/.example.env for setup instructions' or the resolved path to .env.local)",
                "When NODE_ENV === 'production', the check is skipped entirely (no error even if GITHUB_TOKEN is missing)",
                "When GITHUB_TOKEN is set in dev mode, the app starts normally with no warnings",
                "The validation runs in the auth config module (gitstat-app/src/lib/auth.ts) at module load time, not inside a request handler",
                "Typecheck passes"
            ],
            "priority": 5,
            "passes": true,
            "notes": "Implemented: added module-level GITHUB_TOKEN validation in auth.ts that throws at startup in non-production mode"
        }
    ]
}
